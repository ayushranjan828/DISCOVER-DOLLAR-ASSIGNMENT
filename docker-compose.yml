version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "${BACKEND_PORT:-5000}:8080"
    environment:
      - PORT=${BACKEND_PORT:-5000}

  frontend:
    build: ./frontend
    ports:
      - "${FRONTEND_PORT:-8081}:80"
    depends_on:
      - backend
